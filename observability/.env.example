# Credenciales del admin de Grafana. Copia este archivo a .env y cambia el password.
#   cp .env.example .env
# .env está ignorado por git (.gitignore de la raíz ignora .env*), NO se commitea.
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin
